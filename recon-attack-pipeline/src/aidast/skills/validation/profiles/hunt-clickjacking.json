{
  "schema_version": 1,
  "attack_skill_name": "hunt-clickjacking",
  "signal_types": [
    "dom_effect"
  ],
  "runtime_kinds": [
    "browser"
  ],
  "target_expected_signal": {
    "kind": "hunt-clickjacking_verified",
    "criterion": "a framed page permits activation of the declared sensitive UI action",
    "requires_fresh_target_and_control_evidence": true
  },
  "control_positive": {
    "payload_template": {
      "mode": "channel_health_baseline"
    },
    "expected_signal": {
      "kind": "dom_effect_channel_operational",
      "criterion": "the same transport, identity, parser, browser, callback, or timing channel produces its declared harmless baseline"
    },
    "signal_type": "dom_effect"
  },
  "control_negative": {
    "payload_template": {
      "mode": "inert_same_shape_control"
    },
    "expected_signal": {
      "kind": "no_dom_effect_target_effect",
      "criterion": "an inert same-shape input does not produce the target security effect"
    }
  },
  "impact_rules": {
    "boundary": "identify the origin, user-interaction, or victim-context boundary required by the browser effect",
    "sensitivity": "distinguish inert markup from script execution or a sensitive UI action",
    "actor_requirements": "score required delivery, victim interaction, and authentication state"
  },
  "allowed_development_actions": [
    {
      "action_type": "prepare_declared_browser_state",
      "blocker_axis": "state_setup"
    },
    {
      "action_type": "apply_profile_allowed_encoding",
      "blocker_axis": "encoding_transport"
    }
  ],
  "impact_expansion_paths": []
}
