{
  "schema_version": 1,
  "attack_skill_name": "hunt-xxe",
  "signal_types": [
    "oob_callback"
  ],
  "target_expected_signal": {
    "kind": "hunt-xxe_verified",
    "criterion": "the controlled callback or local marker proves external entity resolution",
    "requires_fresh_target_and_control_evidence": true
  },
  "control_positive": {
    "payload_template": {
      "mode": "channel_health_baseline"
    },
    "expected_signal": {
      "kind": "oob_callback_channel_operational",
      "criterion": "the same transport, identity, parser, browser, callback, or timing channel produces its declared harmless baseline"
    },
    "signal_type": "oob_callback"
  },
  "control_negative": {
    "payload_template": {
      "mode": "inert_same_shape_control"
    },
    "expected_signal": {
      "kind": "no_oob_callback_target_effect",
      "criterion": "an inert same-shape input does not produce the target security effect"
    }
  },
  "impact_rules": {
    "boundary": "identify whether the callback crosses the target server or internal network boundary",
    "sensitivity": "distinguish DNS-only reachability from authenticated data transfer or local resource access",
    "actor_requirements": "score the minimum caller role and callback prerequisites"
  },
  "allowed_development_actions": [
    {
      "action_type": "refresh_callback_registration",
      "blocker_axis": "state_setup"
    },
    {
      "action_type": "adjust_callback_wait_window",
      "blocker_axis": "timing_concurrency"
    }
  ],
  "impact_expansion_paths": []
}
