# Juice Shop 인증 Recon 단일 실행 — 2026-09-24

## 실행 조건

- 대상: 고정된 Juice Shop 이미지 `sha256:73c53fbf442e8337b3ea3d98c7e8550308854701ebdfce4cc39768f36b75430e`
- 승인 Scope: `scope_local_lab_juice_shop`, canonical asset `http://127.0.0.1:3001/`
- 인증: 기존 `primary` 세션 번들의 무결성 검증 및 실행 전 `GET /rest/user/whoami` HTTP 200 확인
- scan ID: `scan_cb8cf297f5914274864786fbad3b0a80`
- 정책: GET/HEAD/OPTIONS, 0.5 RPS, concurrency 1, depth 2, 요청 상한 150, timeout 15초
- ffuf: wordlist를 지정하지 않아 실행하지 않음
- 태깅: 외부 Codex 모델로 인증 관측 메타데이터가 전달될 수 있다는 자동 승인 심사 거절로 실행하지 않음
- 원본: `result/test-runs/09.24/no-ffuf-150/{Recon.db,Surface.json}` 및 `result/test-runs/09.24/logs/<scan_id>/recon.jsonl`

## 관측 결과

| 항목 | 결과 |
| --- | ---: |
| scan | `completed` |
| Origin | `http://127.0.0.1:3001/`, probe HTTP 200, Angular SPA |
| HTTP transaction | 125건, 모두 `127.0.0.1:3001` |
| DB endpoint | 26개: GET 24개(제외 8개), POST 2개 |
| Surface endpoint | 18개 |
| Endpoint observation | 208개 |
| 파라미터 | 14개 |
| 예산 보류 후보 | 64개 |
| annotation run / tag | 0개 / 0개 |

실행 중 `GET /rest/user/whoami`는 4건, `GET /rest/basket/8`은 2건의 HTTP 200 transaction과 연결됐다. 인증된 경로의 관측 증거가 있다. POST 7건은 프록시에 수동 관측된 Socket.IO 트래픽이며, 능동 POST 탐색 결과로 세지 않는다.

별도 [고정 GET 정답지](JUICE_SHOP_GET_GROUND_TRUTH.json)의 71개 중 9개에 적중했다(12.7%). 적중 집합은 `/`, `/api/Challenges`, `/api/Quantitys`, `/rest/admin/application-configuration`, `/rest/admin/application-version`, `/rest/basket/:id`, `/rest/languages`, `/rest/products/search`, `/rest/user/whoami`다. 이는 **전체 등록 GET route 대비 일치율**이다. 인증과 시작 경로를 반영한 eligible route 분모가 아직 없으므로 이 실행의 탐색 가능 경로 recall로 해석하지 않는다. 전체 O/X와 누락 경로는 [비교표](RECON_GET_ROUTE_MATRIX.md)에 있다.

## 공격표면 오탐 후보

다음 5개 `Surface.json` 경로는 템플릿 표현식이 URL로 인코딩된 형태다.

```text
/%7B%7Bhref%7D%7D
/%7B%7Bhref%7D%7D/socket.io/
/assets/i18n/%7B%7BblueSky%7D%7D
/assets/i18n/%7B%7Bquestionnaire%7D%7D
/assets/i18n/%7B%7Breddit%7D%7D
```

이들의 저장된 응답은 모두 HTTP 200 `text/html`이며, 9393바이트 응답 본문의 SHA-256 앞 12자리가 루트 HTML과 같은 `aa97229042ec`다. 별도 애플리케이션 경로로 확인된 응답이 아니라 SPA fallback으로 판단된다. 200 상태 코드만으로 경로 존재를 확정하면 이 5개가 공격표면에 남는다. 원본 관측을 보존하면서 후보의 최종 Surface 포함 여부를 판정하는 로직을 점검할 필요가 있다.

## 판정과 한계

- Origin 식별, 인증 경로 관측, Scope 내 HTTP transaction 저장, Surface 출력은 확인했다.
- 기준 GET route 적중 수는 이전 인증 실행의 9/71과 같다. 단일 실행이므로 안정성이나 성능 향상·저하는 판단하지 않는다.
- 태깅 실행 및 라벨 정확도는 `NOT_MEASURED`다. 외부 전송을 승인받거나 별도의 로컬 평가 방법을 정하기 전까지 인증 관측을 모델에 보내지 않는다.
- 태깅 대상 메타데이터 208건에서 기존 세션 토큰 문자열과 JWT 형태 문자열 일치가 각각 0건임을 로컬에서 확인했다. 이는 태깅 입력 필드에 대한 점검이며 외부 전송 승인으로 취급하지 않는다.
- 다음 Recon 수정 후보는 템플릿 URL 및 SPA fallback 응답이 Surface에 포함되는 경로다. 수정 전 해당 경로의 원본 observation과 transaction 관계를 보존해야 한다.

## ffuf wordlist 확인 실행

기존 `resources/wordlists/common.txt`의 4,751개를 모두 실행하지 않고, 이 목록에 실제로 있는 11개 항목을 `result/test-runs/09.24/wordlists/ffuf-smoke-wordlist.txt`로 고정했다. 공개적으로 알려진 경로도 포함한 기능 확인용 목록이므로, 이 결과를 전체 wordlist의 탐지율로 해석하지 않는다. 인증 세션·0.5 RPS·depth 2·concurrency 1은 위 실행과 같고 `--ffuf-wordlist`를 지정했다. 태깅은 두 실행 모두 하지 않았다.

| Scan | 요청 상한 | ffuf 루트 | ffuf 반환 후보 | 대상에 도달한 wordlist 형태 경로 | 기준 GET route 적중 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `scan_e389332715b649a9b37c16e42810a5af` | 150 | 3개 | 0개 | 0개 | 9/71 |
| `scan_b036c505db6449c8a8e11dd6965f5f6c` | 300 | 4개 | 8개 | 고유 33개 | 12/71 |

150 상한 실행에서 ffuf 프로세스 세 개는 모두 exit code 0이었지만, 11개 단어 × 3개 루트의 요청 33건이 전부 `deferred_candidates.reason=request_budget`으로 기록됐다. 프록시는 전체 예산의 20%를 브라우저/API 요청용으로 남기므로 ffuf 같은 낮은 우선순위 요청은 120건에서 막혔다. ffuf 이전 단계가 이 몫을 이미 사용해 ffuf의 실제 대상 요청이 0건이었다. 프로세스 정상 종료와 탐색 수행을 같은 의미로 취급하면 안 된다.

300 상한 실행에서는 ffuf가 `/`, `/api`, `/rest`, `/socket.io` 네 루트를 선택했다. `/`에서 후보 8개가 나왔고 다른 루트에서는 0개였다. 반환 후보의 상태는 HTTP 200 네 개, 301 한 개, 500 세 개다. 마지막 `/socket.io` 루트의 wordlist 요청은 예산으로 보류됐다. 두 실행의 루트 선택 결과가 다르므로 후보 수를 순수한 요청 상한 효과로 비교하지 않는다.

ffuf가 추가로 맞힌 고정 GET route는 `/.well-known/security.txt`(200), `/security.txt`(200), `/profile`(500) 세 개다. `/profile`은 **route 발견**에는 포함하지만 정상 응답을 확인한 경로에는 포함하지 않는다. 나머지 ffuf 후보에는 `/ftp`·`/robots.txt` 같은 정적·middleware 경로, `/assets` 리디렉션, 500 응답의 `/api`·`/rest`가 있다. 300 상한 실행의 저장된 HTTP transaction 239건은 모두 `127.0.0.1:3001`이었다. 원본 DB와 Surface는 `result/test-runs/09.24/smoke-ffuf-150/` 및 `result/test-runs/09.24/smoke-ffuf-300/`에 분리 보관했다.

이번 확인으로 ffuf 자체의 실행 경로는 동작함을 확인했다. 다만 적은 예산에서 앞선 수집기가 ffuf 몫을 소진하는 스케줄링 문제와, 반환 후보를 실질 공격표면으로 분류할 때 응답 상태·경로 유형을 검증해야 하는 문제가 남는다.
