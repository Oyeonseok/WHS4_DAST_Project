# Demo never contacts any target or backend. Change explicitly for integration.
VITE_TRANSPORT=demo
# Required when VITE_TRANSPORT=live:
# VITE_SCAN_ID=your_scan_id
# Optional; defaults to the browser's origin. Use a same-origin proxy in production.
# VITE_API_BASE_URL=http://127.0.0.1:8000
# VITE_WS_BASE_URL=ws://127.0.0.1:8000
# Never store secrets in VITE_* variables: they are public build-time values.
